<local:BaseWindow
    x:Class="LenovoLegionToolkit.WPF.Windows.Utils.ArgumentWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:LenovoLegionToolkit.WPF.Controls"
    xmlns:custom="clr-namespace:LenovoLegionToolkit.WPF.Controls.Custom"
    xmlns:local="clr-namespace:LenovoLegionToolkit.WPF.Windows"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:utils="clr-namespace:LenovoLegionToolkit.WPF.Utils"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{x:Static resources:Resource.ArgumentWindow_Title}"
    Width="800"
    Height="650"
    MinWidth="800"
    MinHeight="650"
    Background="Transparent"
    FlowDirection="{x:Static utils:LocalizationHelper.Direction}"
    ResizeMode="CanMinimize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <wpfui:TitleBar
            x:Name="_titleBar"
            Grid.Row="0"
            CanMaximize="false"
            ShowMaximize="false"
            ShowMinimize="false"
            UseSnapLayout="false" />

        <Grid
            Grid.Row="0"
            Height="24"
            Margin="12,8,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                FontSize="12"
                IsHitTestVisible="False"
                Text="{x:Static resources:Resource.ArgumentWindow_Title}" />

        </Grid>

        <ScrollViewer
            Grid.Row="1"
            Margin="20,0,20,0"
            Padding="0,0,10,0"
            VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="_optionsPanel" />
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="20,15,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                VerticalAlignment="Center"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="{x:Static resources:Resource.SettingsPage_RestartRequired_Message}" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <wpfui:Button
                    Margin="0,0,10,0"
                    Appearance="Secondary"
                    Click="Cancel_Click"
                    Content="{x:Static resources:Resource.Close}" />

                <wpfui:Button
                    Appearance="Primary"
                    Click="Save_Click"
                    Content="{x:Static resources:Resource.SaveAndClose}" />
            </StackPanel>
        </Grid>
    </Grid>
</local:BaseWindow>
